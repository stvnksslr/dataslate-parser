---
kind: pipeline
name: default

steps:
  - name: Lint
    image: python:3.7
    commands:
      - pip install flake8
      - flake8
      
  - name: Test
    image: python:3.7
    environment:
      CC_TEST_REPORTER_ID:
        from_secret: CODE_CLIMATE
    commands:
      - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
      - chmod +x ./cc-test-reporter
      - pip install pipenv
      - pipenv install --dev --system
      - ./cc-test-reporter before-build
      - pytest --cov-branch --cov-report xml --cov=app app
      - ./cc-test-reporter after-build --coverage-input-type coverage.py --exit-code $?
---
kind: signature
hmac: 3948515587dbdd881a2a8907913c2a1ca9f9b8cec5de53a21e363e7e295d64c6

...
