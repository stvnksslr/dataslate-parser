---
kind: pipeline
name: default

steps:
  - name: Lint
    image: python:3.7
    commands:
      - pip install --upgrade pip
      - pip install flake8
      - flake8
      
  - name: Test
    image: python:3.7
    environment:
      CC_TEST_REPORTER_ID:
        from_secret: CODE_CLIMATE
    commands:
      - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
      - pip install --upgrade pip
      - pip install poetry
      - chmod +x ./cc-test-reporter
      - poetry install
      - ./cc-test-reporter before-build
      - poetry run pytest --cov-branch --cov-report xml --cov=app app
      - ./cc-test-reporter after-build --coverage-input-type coverage.py --exit-code $?
---
kind: signature
hmac: d7b31e8384997e757b13abf27e567ea4425b43e70be1c16d019f8b48eb83da69

...
